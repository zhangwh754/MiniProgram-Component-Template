<picker value="{{pickerIndex}}" range="{{options}}" range-key="label" bind:change="onPickerChange">
  <display desc="{{desc}}" placeholder="{{placeholder}}" value="{{showValue}}"></display>
</picker>
