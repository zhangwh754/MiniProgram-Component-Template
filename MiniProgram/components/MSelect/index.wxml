<picker value="{{pickerIndex}}" range="{{options}}" range-key="{{labelKey}}" bind:change="onPickerChange">
  <display desc="{{desc}}" placeholder="{{placeholder}}" value="{{showValue}}"></display>
</picker>
