<MTitle title="基础表格" />

<MTable
  scroll="{{false}}"
  columnKey="key"
  columnLabel="title"
  columns="{{columns}}"
  dataSource="{{data}}"
  bind:headerClick="bindHeaderClick"
  bind:action="bindAction"
>
</MTable>

<view wx:if="{{eventInfo}}">{{eventInfo}}</view>
