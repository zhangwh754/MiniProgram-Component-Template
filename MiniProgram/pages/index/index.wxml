<import src="../../template/msgItem.wxml" />

<MTable columns="{{columns}}" dataSource="{{data}}" bind:action="bindAction">
  <view wx:for="{{data}}" wx:key="index" slot="button-slot-{{index}}">
    <button class="button {{index == 1 && 'blue'}}">测试按钮{{index}}</button>
  </view>
</MTable>

<view wx:if="{{eventInfo}}">{{eventInfo}}</view>
